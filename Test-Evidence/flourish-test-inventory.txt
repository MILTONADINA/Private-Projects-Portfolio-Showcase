================================================================
Flourish — Test Surface Inventory (auditable from source)
================================================================

Date captured: 2026-05-26
Source: private repo MILTONADINA/Flourish, depth-1 clone
Method: deterministic file enumeration + grep on real test files
Test runner: Vitest 4.1.5 (Jest also present)

----------------------------------------------------------------
1. Test file count
----------------------------------------------------------------
$ find . -type d -name node_modules -prune \
       -o -type f \( -name "*.test.ts" -o -name "*.test.tsx" -o -name "*.spec.ts" \) -print \
       | grep -v node_modules | wc -l
133

  -> 133 test files

----------------------------------------------------------------
2. it()/test() call count
----------------------------------------------------------------
1121
  -> 1,121 it() / test() call sites

----------------------------------------------------------------
3. CI workflows (real .github/workflows/)
----------------------------------------------------------------
ci.yml
dependency-audit.yml
deploy-api.yml
deploy-mobile-preview.yml
deploy-mobile-release.yml
deploy-web.yml
sbom.yml
secret-scan.yml
semgrep.yml

----------------------------------------------------------------
4. Decision-log + ADR surface
----------------------------------------------------------------
ADR files (docs/03-architecture/adrs/):
25
 ADRs

Decision-log entries (docs/00-project/decision-log.md, ^## D-):
26
 entries (D-017 through D-034+)

----------------------------------------------------------------
Notes
----------------------------------------------------------------
- Flourish is in Phase 0 (foundation / monorepo scaffold).
- 1,121 test call sites with 133 files in Phase 0 reflects the
  test-first discipline established at foundation; not a launched product.
- Client identity remains REDACTED in all public artifacts.
